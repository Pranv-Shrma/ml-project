# Its primary purpose is to mark a directory as a Python package. 
# When Python encounters a directory containing __init__.py, 
# it treats that directory as a package rather than just a regular folder.